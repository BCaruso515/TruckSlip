<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TruckSlip.Views.SignFormPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:datasource="clr-namespace:TruckSlip.Models"
    xmlns:helpers="clr-namespace:TruckSlip.Helpers"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:vm="clr-namespace:TruckSlip.ViewModels"
    x:Name="this"
    Title="Sign Form"
    x:DataType="vm:SignFormViewModel">

    <!--<ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            BindingContext="{Binding Path=BindingContext, Source={x:Reference this}, x:DataType=Page}"
            Command="{Binding AppearingCommand}"
            EventName="Appearing" />
    </ContentPage.Behaviors>

    <Grid
        Padding="15"
        RowDefinitions="*,Auto"
        RowSpacing="10">
        <CollectionView
            Grid.Row="0"
            ItemsSource="{Binding People}"
            SelectedItem="{Binding SelectedPerson}"
            SelectionChangedCommand="{Binding GetSignatureFromPadCommand}"
            SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="datasource:Person">
                    <Border Padding="8" StrokeShape="RoundRectangle, 15">
                        <Label Text="{Binding FullName}" />
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <HorizontalStackLayout Grid.Row="1" Spacing="15">
            <Button
                Padding="10"
                Command="{Binding ViewPdfCommand}"
                Text="Export Document">
                <Button.ImageSource>
                    <FontImageSource
                        FontFamily="{Binding FontFamily}"
                        Glyph="{x:Static helpers:FontAwesomeHelper.FilePdf}"
                        Size="25" />
                </Button.ImageSource>
            </Button>
        </HorizontalStackLayout>
    </Grid>-->
</ContentPage>